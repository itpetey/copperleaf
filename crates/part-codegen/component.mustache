/// {{{title}}}
{{#description}}
///
/// {{{.}}}
{{/description}}
///
/// # Pinout
///
/// | Pin | Name     | Purpose     | Notes                 |
/// |-----|----------|-------------|-----------------------|
{{#pins}}
/// | {{{row}}} |
{{/pins}}
pub struct {{{struct_name}}} {
    pins: Vec<copperleaf::Pin>,
}

impl {{{struct_name}}} {
    {{#constants}}
    pub const {{{name}}}: copperleaf::PinRef = copperleaf::PinRef("{{{pin_name}}}");
    {{/constants}}

    pub fn new() -> Self {
        use copperleaf::{Pin, Role, units::UnitExt};

        Self {
            pins: vec![
                {{#builders}}
                {{{.}}},
                {{/builders}}
            ],
        }
    }
}

impl copperleaf::Component for {{{struct_name}}} {
    fn pins(&self) -> &[copperleaf::Pin] {
        &self.pins
    }
}

impl Default for {{{struct_name}}} {
    fn default() -> Self {
        Self::new()
    }
}
