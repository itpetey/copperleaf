[package]
name = "copperleaf-backend-kicad"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "KiCad backend (minimal placeholder)"

[dependencies]
copperleaf-ir = { path = "../../ir" }
serde = { version = "1", features = ["derive"] }
